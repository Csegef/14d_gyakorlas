<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DummyJsonRecipes.MainPage"
             Title="ListaPage">

    <Border Margin="10" VerticalOptions="Fill" Stroke="{StaticResource SColor}" BackgroundColor="{StaticResource PColor}" StrokeShape="RoundRectangle 20,20,20,20">

        <CollectionView Margin="10" Grid.Row="2" HorizontalOptions="Center" ItemsSource="{Binding .}" SelectionMode="Single">
        </CollectionView>
    </Border>

</ContentPage>
